name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build packages
        run: bun run build

      - name: Test
        run: bun run test

      # Bundlers tolerate extensionless relative specifiers; Node's ESM loader
      # does not. Without this, a package can pass every other check and still
      # break on import from a real Node process (SSR, externalised deps).
      - name: ESM resolution smoke test
        run: bun run smoke:esm

      - name: Packed tarball smoke test
        run: bun run smoke:pack

      - name: Lint
        run: bun run lint

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build packages
        run: bun run build

      # Must run through the e2e workspace, not `bunx playwright`: the binary
      # is not linked at the repo root, so bunx would fetch playwright@latest
      # from npm and download browsers for a different version than the one
      # running the tests — the browser revisions then disagree and every test
      # fails with "Looks like Playwright was just installed or updated".
      - name: Install Playwright browsers
        run: bun run --cwd e2e install:browsers

      - name: End-to-end tests
        run: bun run e2e

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: e2e/playwright-report/
          retention-days: 7
