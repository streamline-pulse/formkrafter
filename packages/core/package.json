{
  "name": "@streamline-pulse/formkrafter-core",
  "version": "0.0.1",
  "description": "Framework-agnostic core for FormKrafter",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && bun build lib/index.ts --outdir dist --format esm --packages external --sourcemap=linked && tsc -p . --emitDeclarationOnly",
    "test": "bun test"
  },
  "dependencies": {
    "acorn": "catalog:",
    "ajv": "catalog:",
    "ajv-errors": "catalog:",
    "json-logic-js": "catalog:",
    "fast-json-patch": "catalog:",
    "ajv-formats": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/json-logic-js": "catalog:"
  }
}
