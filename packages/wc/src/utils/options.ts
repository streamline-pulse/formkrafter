// Moved to core so DOM-free renderers share the exact same option parsing;
// this re-export keeps every wc import unchanged.
export { normalizeOptions } from '@streamline-pulse/formkrafter-core';
export type { SelectOption } from '@streamline-pulse/formkrafter-core';
